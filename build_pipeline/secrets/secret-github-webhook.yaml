apiVersion: v1
kind: Secret
metadata:
  name: github-webhook-secret
  namespace: janine-dev
type: Opaque
stringData:
  # GitHub webhook signing secret (HMAC). Must match the secret configured on the
  # GitHub repo/org webhook that posts to the Tekton EventListener route.
  secret: "TODO: insert your secret"
