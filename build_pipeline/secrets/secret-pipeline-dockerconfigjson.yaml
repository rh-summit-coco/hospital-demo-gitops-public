apiVersion: v1
kind: Secret
metadata:
  name: pipeline-dockerconfigjson
  namespace: janine-dev
type: kubernetes.io/dockerconfigjson
stringData:
  # Registry credentials JSON. For Quay, use a robot account or user + token.
  # Generate auth with: echo -n 'user:password' | base64 -w0
  .dockerconfigjson: |
    {
      "auths": {
        "quay.io": {
          "username": "TODO: insert your secret",
          "password": "TODO: insert your secret",
          "auth": "TODO: insert your secret"
        }
      }
    }
